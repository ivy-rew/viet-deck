# Viet Deck

```bash
./split.sh video.mp4
# manual: remove useless slides (or change threshold+min slide length)

./extract.sh video_slides
# manual (maybe improve scene_text)

./anki_export.sh video_slides


```